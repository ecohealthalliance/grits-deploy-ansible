---

- hosts: prod
  roles:
    - common
    - postfix
    - apache
    - mongodb
    - node
    - s3restore
    - girder
    - grits-api
    - { role: jvm-nlp, sudo_user: "{{ grits_user }}" }
    - { role: diagnostic-dashboard, sudo_user: "{{ grits_user }}" }
  vars_files:
    - secure.prod.yml
  sudo: yes
